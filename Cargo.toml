[package]
name = "nocc"
version = "0.1.0"
edition = "2024"

[dependencies]
windows = { path = "./windows" }

[workspace]
members = [
    "windows"
]

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true  